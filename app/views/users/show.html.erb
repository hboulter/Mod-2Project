<h1>Donations</h1>
<ul>
<% if @donations.size > 0 %>
    <% @donations.each do |donation| %>
    <li><%= donation.toy.name %></li>
    <% end  %> 
<% else %>
    No donations have been made yet!<br><br>
    <%= link_to 'Donate Now', children_path %>
<% end %>
</ul>

